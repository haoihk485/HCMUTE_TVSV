import ModalLayout from "../../components/modal_layout"

const AddFieldDepModal = ({ handleClose }) => {
    return (
        <ModalLayout handleClose={handleClose} title={'Thêm lĩnh vực cho phòng ban'}>

        </ModalLayout>
    )
}

export default AddFieldDepModal